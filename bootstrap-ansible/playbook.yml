---
- name: Bootstrap Fedora System
  hosts: localhost
  become: true
  collections:
    - community.general
  vars_files:
    - group_vars/all.yml
  roles:
    - packages
    - dotfiles
    - fonts
    - docker
    - node-proton
    - gnome
